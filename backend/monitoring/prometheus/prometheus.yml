global:
  scrape_interval: 15s

scrape_configs:
  - job_name: agent-arena
    metrics_path: /actuator/prometheus
    static_configs:
      # 컨테이너에서 호스트의 로컬 앱(8081)을 스크래핑한다.
      - targets: ["host.docker.internal:8081"]
